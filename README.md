# AgroMontes MVP

Este es un proyecto [Next.js](https://nextjs.org) creado con [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## 🚀 Guía de Instalación y Ejecución

Sigue estos pasos para configurar y ejecutar el proyecto en una computadora nueva.

### 1. Prerrequisitos

Antes de empezar, necesitas instalar lo siguiente:

*   **Node.js**: Es el entorno necesario para ejecutar JavaScript fuera del navegador.
    *   Descarga la versión **LTS (Recommended)** desde aquí: [https://nodejs.org/](https://nodejs.org/)
    *   Instálalo siguiendo los pasos del asistente (siguiente, siguiente, instalar).
*   **Git** (Opcional pero recomendado): Para descargar el código fácilmente.
    *   Descárgalo desde: [https://git-scm.com/downloads](https://git-scm.com/downloads)

### 2. Descargar el Proyecto

Si tienes Git instalado, abre una terminal (o CMD) y ejecuta:

```bash
git clone <URL_DEL_REPOSITORIO>
cd agromontes-mvp
```

Si descargaste el archivo ZIP:
1.  Descomprime el archivo.
2.  Abre la carpeta `agromontes-mvp`.
3.  Haz clic derecho en un espacio vacío y selecciona "Abrir en Terminal" (o abre CMD y navega a la carpeta).

### 3. Instalar Dependencias

En la terminal, dentro de la carpeta del proyecto, ejecuta el siguiente comando para descargar todas las librerías necesarias:

```bash
npm install
```

*Esto puede tardar unos minutos dependiendo de tu conexión a internet.*

### 4. Ejecutar el Proyecto

Una vez instaladas las dependencias, inicia el servidor de desarrollo con:

```bash
npm run dev
```

Verás un mensaje indicando que el servidor está listo, usualmente en `http://localhost:3000`.

### 5. Ver la Página

Abre tu navegador web (Chrome, Edge, Firefox) e ingresa a:

[http://localhost:3000](http://localhost:3000)

¡Listo! Deberías ver la página de AgroMontes funcionando.

---

## Comandos Útiles

*   `npm run dev`: Inicia el servidor de desarrollo.
*   `npm run build`: Construye la aplicación para producción.
*   `npm start`: Inicia el servidor de producción (después de ejecutar build).
*   `npm run lint`: Revisa el código en busca de errores.
